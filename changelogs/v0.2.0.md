Update to v0.2.0

Description
-----------

Starting with this release wgadmin supports both `.json` and `.yml` config files with the latter being the default.

Included Commits
----------------

- 3e87a49: Format code.
- cbd0b8e: Support both json and yaml for the config file with yaml being the default (for easy integration with ansible).
